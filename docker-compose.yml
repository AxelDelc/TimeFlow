services:
  app:
    build: .
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - PORT=3000
    volumes:
      # Persiste la base de données SQLite entre les redémarrages du conteneur
      - ./dev.db:/app/dev.db
    restart: unless-stopped
